schema: nenjo.modules.v1
resources:
  - schema: nenjo.context_block.v1
    manifest:
      name: methodology
      template: Think through the work carefully.
  - schema: nenjo.context_block.v1
    manifest:
      name: tool_usage
      imports:
        context:
          - "#methodology"
      template: |
        {{ @acme/core/methodology }}
        Use tools when they provide current evidence.
