schema: nenjo.agent.v1
slug: support-agent
imports:
  abilities:
    - ../abilities/design.yaml#design_agent
manifest:
  name: support_agent
  prompt_config:
    system_prompt: |
      {{ pkg.acme.core.methodology }}
